version: "3"
services:
  movenet:
    build: .
    volumes:
      - .:.
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]